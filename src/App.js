import "./App.css";
import Meme from "./components/Meme";

function App() {
  return <div>
    <Meme/>
  </div>;
}

export default App;


// we use npm run build commad for production
// which created a build folder
// we deploy the build folter to our serverr